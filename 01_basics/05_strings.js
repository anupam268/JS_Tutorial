const name = "anupam"
const place = "bangalore"

console.log(name+place) // outdated
console.log(`${name} ${place}`) //more readable. it uses back-tick

const position = new String("lead");

console.log(position.length);
console.log(position);
console.log(position.toUpperCase());
console.log(position.toUpperCase);